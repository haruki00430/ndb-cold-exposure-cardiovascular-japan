# Python dependencies for ndb-cold-exposure-cardiovascular-japan
# Tested on Python 3.14 (Windows 11)
# Install: pip install -r requirements.txt

# Core data manipulation
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.0         # Excel file reading (.xlsx)
pyyaml>=6.0.0           # Config file parsing

# Statistical analysis
scipy>=1.12.0
statsmodels>=0.14.0
scikit-learn>=1.4.0

# Spatial analysis
geopandas>=0.14.0
libpysal>=4.10.0
esda>=2.6.0
spreg>=1.5.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
pillow>=10.0.0           # Figure export (PNG at 500 DPI)

# Data acquisition
requests>=2.31.0

# Note: ndb_library (set_japanese_font etc.) is a local shared library
# located at ../../src/ndb_library/ relative to this project root.
# It is not distributed via PyPI. Copy or symlink as needed.
